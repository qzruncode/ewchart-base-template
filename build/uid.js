const subAppName = 'darkTunnelBaseTemplate'; // 此子应用的名称
const hasMaster = !!(window.appState && window.appState[subAppName]);

if (hasMaster) {
  window.appState[subAppName].uid = '16822481323335hxra77n0kd';
}
